# ActiveUsersStats represents the active user card in overview graph
type ActiveUsersStat {
    Gauge: Gauge
    Trend: Trend
    Sparkline: [Point]
}

# Cluster query manage 
type Query {
    activeUsers(start: String, stop: String, every: String): ActiveUsersStat
}